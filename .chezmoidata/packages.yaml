packages:
  darwin:
    brews:
      - git # yes
      - argocd # need the cli
      - jq # pipe it all
      - gh
      - chezmoi # this
      - helm # packages
      - kubecm # config manager
      - kubectx # context switch
      - kubernetes-cli # duh
      - ncdu # disk usage ui
      - nmap # spy
      - ollama # local models
      - sqlmap
      - sqlite # local storage
      - suricata # spy
      - fzf # great fuzzy search
      - duf # better disk usage report
      - tealdeer # man page steroids
      - bat # super cat
      - eza # better ls
      # mosh ? ssh replace
      # glances ? top replace
      # btop ? top replace
      # dust ? du replace
      # atuin ? shell history in sqlite with more context
      - yq
      - jq
      - nvim
      # dog ? dig replacement
      # himalaya - cli email?
      # procs ? ps replacement
      - sketchybar

    casks:
      - itermai
      - brave-browser
      - gpg-suite
      - little-snitch
      - mattermost
      - iterm2
      - visual-studio-code
      - aerospace
      - font-hack-nerd-font
      - font-sketchybar-app-font
      - sf-symbols
      - bitwarden
    taps:
      - nikitabobko/tap
      - FelixKratz/formulae
